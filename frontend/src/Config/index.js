const BASE_URL_LOGIN = "http://localhost:3000"; // Local Node.js backend
const BASE_URL_ALL = "http://localhost:3000"; // Same backend for all APIs for now


const CONFIG = {
    BASE_URL_LOGIN,
    BASE_URL_ALL,
    
}

export default CONFIG;
